in my data wranging. i combined and prepared multiple datasets to ceate a clean, unified output. i started by cleaning the reginal planing dataset and then used a union step to integrate it with other relevent data. seperately, i cleaned the base layer rental data and reshaped it using a pivot transformation to ensure the structure matched the required format. These datasets were then merged using another union step. in parallel, i cleaned the public housing dataset and combined it with the previously merged data through an additional union. finally, i performed a last cleaning step to standardize fieldss, remove inconsitencies, and ensure data quality before generating the final output.
